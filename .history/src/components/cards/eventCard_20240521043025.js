import { Card, CardBody, CardHeader } from "@nextui-org/react";

export default function EventCard(){
    return(
        <Card>
            <CardHeader>
                <h4 className="font-extrabold flex justify-center">SỰ KIỆN</h4>
            </CardHeader>
        </Card>
    );
}